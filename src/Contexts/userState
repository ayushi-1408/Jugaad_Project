import react from 'react';
import UserContext from './UserContext';

const UserState = (props) => {
    const state = {
        email:"harshita.1470@gmail.com",
        password:"harshita"
    }

    return (
        <UserContext.Provider value="abcd">
            {props.children}
        </UserContext.Provider>
    )
}

export default UserState